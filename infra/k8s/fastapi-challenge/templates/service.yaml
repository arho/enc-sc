apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastapi-challenge.fullname" . }}
  labels:
    {{- include "fastapi-challenge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fastapi-challenge.selectorLabels" . | nindent 4 }}
